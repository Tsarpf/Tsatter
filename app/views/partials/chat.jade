span(ng-controller="ChatController as chatCtrl", class="chat vspace")

    .row.tscontainer

        .users.col-xs-2
            p(ng-repeat="user in users")

        div(class="messages col-xs-8", ng-attr-id="{{room}}")

            ts-chat-message(ng-repeat="message in messages")
                
            
        .tsmedia.col-xs-2

        input(ng-click="chatCtrl.clicked()", ng-keyup="$event.keyCode == 13 && chatCtrl.sendMsg()", ng-model="msg", type="text", class="newMessage col-xs-offset-2 col-xs-8")

